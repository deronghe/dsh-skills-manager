# 待办与阻塞

更新时间：2026-08-17 16:10（Asia/Shanghai）

## 待办事项

### I030 上传链式守卫回归实机验收

- 负责人：YUJIYU
- 优先级：高
- 下一步：安装 `@michengai/dsh-skills-manager@0.1.23` 并重启 DSH，上传无冲突技能目录，确认实导发出且不再卡「上传中」。
- 解除条件：无冲突直传、重名覆盖两条路径均实机通过。

### I029 代码评审修复实机验收

- 负责人：YUJIYU
- 优先级：中
- 下一步：安装 `@michengai/dsh-skills-manager@0.1.22` 并重启 DSH，验证上传含符号链接来源时在选择阶段即报错、删除与启停正常。
- 解除条件：dry-run 预检即结论的路径实机符合预期。

### 技能列表搜索实机验收

- 负责人：YUJIYU
- 优先级：中
- 下一步：在设置 → 技能中输入名称/简介，切换「全部 / DSH / 公共 Agent」，确认列表即时过滤，无结果时可清除筛选。
- 解除条件：中英文界面检索、分类收窄和窄屏换行符合预期。

### 上传弹窗紧凑样式实机验收

- 负责人：YUJIYU
- 优先级：高
- 下一步：确认「上传插件」是约 480px 居中紧凑卡片，投放区不会铺满设置页。
- 解除条件：弹窗按内容收高度，窄屏按钮可换行。

### 斜杠菜单即时刷新实机验收

- 负责人：YUJIYU
- 优先级：高
- 下一步：安装含 I023 的版本后，启停或上传一个技能，不按 F5，确认聊天 `/` 列表立即变化。
- 解除条件：启停和安装后无需刷新页面即可召唤或隐藏对应技能。

### 本机安装与端到端验证

- 负责人：YUJIYU
- 优先级：高
- 下一步：安装 `@michengai/dsh-skills-manager@0.1.20` 并重启 DSH，验证上传、覆盖、启停、删除、公共 Agent 目录只读，以及中英文切换（设置 → 通用 → Language）。
- 解除条件：面板可操作，接口全链路通过，双语界面符合预期。

### 官方技能目录失效事件

- 负责人：YUJIYU
- 优先级：低
- 下一步：若 DSH 将 `skills/change` 加入 Web 转发白名单，删除对 `agent-preset/selected` 的借用。
- 解除条件：写入后只发官方目录失效事件，斜杠菜单仍即时刷新。

### 创建/编辑能力

- 负责人：YUJIYU
- 优先级：中
- 下一步：设计新建 skill 模板与在线编辑 frontmatter/正文的交互与接口。
- 解除条件：需求范围经确认后进入实施。

### 原生文件选择器与弹窗交互

- 负责人：YUJIYU
- 优先级：低
- 下一步：确认实际 DSH 桌面环境优先返回 `SKILL.md` 路径，或可经原生目录选择器返回插件目录；确认 ESC 只关闭最上层弹窗。
- 解除条件：两条上传路径至少一条在目标 DSH 版本可用，且嵌套弹窗关闭行为符合预期。

## 阻塞事项

无。


